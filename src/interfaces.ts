export interface Store {
    name?: string;
    img?: string;
    opened?: boolean;
    delivery_fee?: number;
    distance?: number;
    description?: string;
}

export interface Categoria {
    name: string;
    img: string;
}
